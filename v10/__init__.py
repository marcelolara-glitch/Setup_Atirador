"""v10 — alicerce do ciclo v10. NADA aqui roda em produção ainda.

Contém apenas contratos: schema da tabela nova (`schema`), a especificação
de setup (`spec`), os adaptadores de saída (`exits`) e a fonte única de
velas (`data`). Nenhum módulo v9 importa daqui.
"""
